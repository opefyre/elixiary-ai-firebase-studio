rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARILY DISABLED FOR WEBHOOK TESTING
    // Allow all reads and writes for testing webhook functionality
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

